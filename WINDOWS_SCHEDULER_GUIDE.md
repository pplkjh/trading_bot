# Windows 작업 스케줄러 설정 가이드

자동 매매 시스템을 완전 자동화하기 위한 Windows 작업 스케줄러 설정 방법입니다.

---

## 📋 목차

1. [전체 시스템 흐름](#전체-시스템-흐름)
2. [사전 준비사항](#사전-준비사항)
3. [작업 스케줄러 기본 사용법](#작업-스케줄러-기본-사용법)
4. [자동화 작업 설정](#자동화-작업-설정)
5. [문제 해결](#문제-해결)

---

## 🔄 전체 시스템 흐름

```
08:30  → BIOS로 컴퓨터 자동 켜짐
08:35  → 키움 자동 로그인 (Windows 시작 프로그램)
08:40  → Collector 실행 (작업 스케줄러) - 데이터 수집
08:50  → Trader 실행 (작업 스케줄러) - 자동 매매 시작
15:30  → Trader 자동 종료 (장 마감 후 자동 감지)
15:35  → 수집기 실행 (작업 스케줄러) - 당일 데이터 수집
21:00  → 스마트 종료 (작업 스케줄러) - 평일: 21시, 주말: 즉시
```

---

## ✅ 사전 준비사항

### 1. BIOS 설정 (컴퓨터 자동 켜기)

**BIOS 진입 방법**:
- 컴퓨터 부팅 시 `Del`, `F2`, `F10` 중 하나 누르기 (메인보드마다 다름)

**설정할 항목**:
1. `Power Management` 또는 `ACPI` 메뉴 찾기
2. `Wake on RTC Alarm` 또는 `Power On by RTC` 활성화
3. 시간 설정: **매일 08:30**
4. 설정 저장 후 재부팅

> **주의**: BIOS 메뉴는 메인보드 제조사마다 다릅니다.
> 제조사별 매뉴얼을 참고하세요.

### 2. 키움 자동 로그인 설정

참고 파일: `batch/auto_shutdown.bat` (이미 해결하셨다고 하셨으니 PASS)

**방법**:
1. 키움 자동 로그인 프로그램 사용
2. 또는 Windows 시작 프로그램에 키움 실행 배치 파일 등록

---

## 🎯 작업 스케줄러 기본 사용법

### Windows 작업 스케줄러 열기

**방법 1**: 검색
```
윈도우 키 → "작업 스케줄러" 검색
```

**방법 2**: 실행 창
```
윈도우 키 + R → taskschd.msc 입력 → 확인
```

### 작업 만들기 기본 순서

**방법 1: XML 파일로 가져오기 (권장) ⭐**

이 프로젝트의 `scheduler/` 폴더에 미리 준비된 XML 파일 4개가 있습니다:

```
scheduler/
├── TradingBot_Morning_Collector.xml    (08:40 - 아침 데이터 수집)
├── TradingBot_Auto_Trading.xml         (08:50 - 자동 매매)
├── TradingBot_Evening_Collector.xml    (15:35 - 저녁 데이터 수집)
└── TradingBot_Smart_Shutdown.xml       (20:50 - 스마트 종료)
```

**가져오기 방법**:
1. 작업 스케줄러 열기
2. 왼쪽의 **작업 스케줄러 라이브러리** 우클릭
3. **작업 가져오기** 클릭
4. `scheduler/TradingBot_Morning_Collector.xml` 선택 → 열기
5. **확인** 클릭 (경로는 자동으로 `%USERPROFILE%\trading_bot`로 설정됨)
6. 나머지 3개 XML 파일도 동일하게 가져오기

> **중요**: XML 파일을 가져온 후 **반드시 각 작업의 속성을 열어서** Python 경로가 본인 환경과 맞는지 확인하세요!

**방법 2: 수동으로 작업 만들기**

1. **작업 스케줄러 라이브러리** 우클릭
2. **기본 작업 만들기** 클릭
3. 이름/설명 입력
4. 트리거(시간) 설정
5. 동작(실행 프로그램) 설정
6. 완료

---

## ⚙️ 자동화 작업 설정

### 🚀 빠른 시작 (XML 가져오기)

**권장 방법**: 위의 "방법 1: XML 파일로 가져오기"를 사용하여 4개의 작업을 한 번에 설정하세요.

1. `TradingBot_Morning_Collector.xml` 가져오기
2. `TradingBot_Auto_Trading.xml` 가져오기
3. `TradingBot_Evening_Collector.xml` 가져오기
4. `TradingBot_Smart_Shutdown.xml` 가져오기

가져오기 완료 후 아래의 "작업 확인 및 테스트" 섹션으로 이동하세요.

---

### 📝 수동 설정 가이드 (참고용)

XML 파일을 사용하지 않고 수동으로 설정하려면 아래 4개의 작업을 **순서대로** 만들어야 합니다.

---

### 작업 1: 아침 데이터 수집 (Collector)

#### 기본 정보
- **이름**: `TradingBot_Morning_Collector`
- **설명**: `아침 장 시작 전 데이터 수집`

#### 트리거 설정
1. **새로 만들기** 클릭
2. 설정:
   - **작업 시작**: `일정`
   - **빈도**: `매일`
   - **시작 시간**: `08:40` (키움 로그인 후 5분 뒤)
   - **사용**: ✅ 체크

#### 동작 설정
1. **새로 만들기** 클릭
2. 설정:
   - **동작**: `프로그램 시작`
   - **프로그램/스크립트**: `C:\Python\python.exe`
   - **인수 추가**: `collector_advanced.py`
   - **시작 위치**: `C:\Users\[사용자명]\trading_bot`

> **중요**: 경로는 본인 환경에 맞게 수정하세요!

#### 조건 설정
- **컴퓨터의 전원이 켜져 있을 때만 작업 시작**: ✅ 체크
- **AC 전원을 사용할 경우에만 작업 시작**: ❌ 체크 해제

#### 설정 탭
- **작업이 실패할 경우 다시 시작 간격**: `1분`
- **다시 시작 시도 횟수**: `3`

---

### 작업 2: 자동 매매 시작 (Trader)

#### 기본 정보
- **이름**: `TradingBot_Auto_Trading`
- **설명**: `자동 매매 프로그램 실행`

#### 트리거 설정
- **시작 시간**: `08:50` (Collector 실행 후 10분 뒤)
- 나머지는 작업 1과 동일

#### 동작 설정
- **프로그램/스크립트**: `C:\Python\python.exe`
- **인수 추가**: `trader_advanced.py`
- **시작 위치**: `C:\Users\[사용자명]\trading_bot`

#### 조건/설정
- 작업 1과 동일

---

### 작업 3: 장 마감 후 데이터 수집

#### 기본 정보
- **이름**: `TradingBot_Evening_Collector`
- **설명**: `장 마감 후 데이터 최종 수집`

#### 트리거 설정
- **시작 시간**: `15:35` (장 마감 후 5분 뒤)
- 나머지는 작업 1과 동일

#### 동작 설정
- **프로그램/스크립트**: `C:\Python\python.exe`
- **인수 추가**: `collector_advanced.py`
- **시작 위치**: `C:\Users\[사용자명]\trading_bot`

---

### 작업 4: 스마트 자동 종료

#### 기본 정보
- **이름**: `TradingBot_Smart_Shutdown`
- **설명**: `주말/평일 구분 자동 종료`

#### 트리거 설정
- **시작 시간**: `20:50` (21시 10분 전)
- 나머지는 작업 1과 동일

#### 동작 설정
- **프로그램/스크립트**: `C:\Windows\System32\cmd.exe`
- **인수 추가**: `/c "C:\Users\[사용자명]\trading_bot\batch\smart_shutdown.bat"`
- **시작 위치**: `C:\Users\[사용자명]\trading_bot\batch`

#### 설정 탭 (추가 설정)
- **작업이 요청 시 중지되지 않으면 강제 중지**: ✅ 체크
- **제한 시간**: `2시간`

---

## 🔍 작업 확인 및 테스트

### 1. 작업 목록 확인

작업 스케줄러 라이브러리에서 다음 4개가 보여야 합니다:
```
✅ TradingBot_Morning_Collector    (08:40)
✅ TradingBot_Auto_Trading         (08:50)
✅ TradingBot_Evening_Collector    (15:35)
✅ TradingBot_Smart_Shutdown       (20:50)
```

### 2. 수동 테스트

각 작업을 우클릭 → **실행**하여 정상 작동 확인:

1. **Morning_Collector**:
   - 실행 후 `logs/collector.log` 확인
   - 데이터베이스에 오늘 날짜 데이터 확인

2. **Auto_Trading**:
   - 실행 후 `logs/jackbot.log` 확인
   - 매수/매도 로직이 정상 작동하는지 확인

3. **Evening_Collector**:
   - Morning_Collector와 동일

4. **Smart_Shutdown**:
   - 실행 후 메시지 확인
   - 주말이면 30초 후 종료, 평일이면 21시 예약 확인
   - **테스트 후 즉시 취소**: `shutdown /a`

---

## 📊 로그 및 모니터링

### 작업 실행 로그 확인

**방법 1**: 작업 스케줄러에서 확인
```
작업 우클릭 → 속성 → 기록 탭
```

**방법 2**: 이벤트 뷰어
```
윈도우 키 → "이벤트 뷰어" 검색
→ Windows 로그 → 응용 프로그램
→ "작업 스케줄러" 원본 필터링
```

### 프로그램 로그 확인

```
trading_bot/logs/
├── jackbot.log           # Trader 로그
├── collector.log         # Collector 로그
├── trading_events.log    # 매매 이벤트 로그 (신규!)
└── automation_log.txt    # 자동화 스크립트 로그
```

### 성과 분석 보고서 생성

```bash
# 수동 실행
python generate_trading_report.py

# 자동 생성 (작업 스케줄러에 추가 작업 생성)
# 이름: TradingBot_Daily_Report
# 시간: 21:00 (종료 전)
# 동작: python.exe generate_trading_report.py
```

---

## ⚠️ 문제 해결

### 문제 0: UAC (사용자 계정 컨트롤) 창이 뜬다

**증상**:
- 작업 스케줄러에서 프로그램 실행 시 "이 앱이 디바이스를 변경할 수 있도록 허용하겠습니까?" 창이 뜸
- 자동화가 중단되고 사용자 입력 대기

**해결 방법 1: 작업 스케줄러 설정 변경 (권장) ✅**

1. 작업 스케줄러에서 해당 작업 우클릭 → **속성**
2. **일반** 탭에서:
   - ✅ "사용자의 로그온 여부에 관계없이 실행" 선택
   - ✅ "가장 높은 수준의 권한으로 실행" 체크
   - **사용자 계정**: 현재 사용자 (USER) 선택
   - 비밀번호 입력 (Windows 로그인 비밀번호)

3. **조건** 탭에서:
   - ❌ "컴퓨터의 전원이 켜져 있을 때만 작업 시작" 체크 해제

4. **확인** 클릭

> **중요**: "사용자의 로그온 여부에 관계없이 실행" 옵션을 선택하면 UAC 프롬프트 없이 백그라운드에서 실행됩니다!

**해결 방법 2: UAC 레벨 낮추기 (선택사항)**

1. 제어판 → 사용자 계정 → 사용자 계정 컨트롤 설정 변경
2. 슬라이더를 "알리지 않음" 또는 두 번째 단계로 이동
3. 확인

> **주의**: 보안 수준이 낮아지므로 권장하지 않습니다. 방법 1을 우선 사용하세요.

**해결 방법 3: XML 파일 재가져오기**

이미 XML 파일에 `<RunLevel>HighestAvailable</RunLevel>` 설정이 되어 있지만, 가져올 때 계정 정보가 설정되지 않았을 수 있습니다:

1. 작업 삭제
2. XML 다시 가져오기
3. 가져온 후 **즉시** 작업 우클릭 → 속성
4. 일반 탭에서 "사용자의 로그온 여부에 관계없이 실행" 선택
5. 비밀번호 입력

---

### 문제 1: 작업이 실행되지 않음

**원인**:
- Python 경로가 잘못됨
- 작업 디렉토리가 잘못됨
- 권한 문제

**해결**:
1. Python 경로 확인:
   ```bash
   where python
   ```
2. 작업 스케줄러에서 **"가장 높은 수준의 권한으로 실행"** 체크
3. **"사용자의 로그온 여부에 관계없이 실행"** 선택

### 문제 2: Trader가 자동 종료되지 않음

**원인**: Trader가 매수/매도 대기 중

**해결**:
- `trader_advanced.py`는 이미 스마트 종료 기능 탑재:
  - 보유 종목 없음 + 후보 없음 → 60초 후 자동 종료
  - 장 마감 후 → 자동 종료

### 문제 3: 주말에도 21시까지 켜져 있음

**원인**: `smart_shutdown.bat`가 실행되지 않음

**해결**:
1. 배치 파일 경로 확인
2. 작업 스케줄러에서 수동 실행 테스트
3. 로그 확인: `automation_log.txt`

### 문제 4: 컴퓨터가 자동으로 켜지지 않음

**원인**: BIOS RTC Alarm 설정 문제

**해결**:
1. BIOS 재진입하여 설정 확인
2. "Restore AC Power Loss" 설정 확인
3. CMOS 배터리 확인 (방전 시 설정 초기화됨)

### 문제 5: 키움 로그인이 안 됨

**원인**: 키움 자동 로그인 설정 문제

**해결**:
- 사용자께서 이미 해결하셨다고 하셨으니, 현재 방법 유지
- 참고: `batch/auto_shutdown.bat`

---

## 🎉 완성!

위 설정을 완료하면:

✅ **평일**:
- 08:30 - 자동 켜짐
- 08:40 - 데이터 수집
- 08:50 - 자동 매매 시작
- 15:35 - 장 마감 후 데이터 수집
- 21:00 - 자동 종료

✅ **주말**:
- 08:30 - 자동 켜짐
- 20:50 - 스마트 종료 스크립트 실행
- 20:51 - 주말 감지 후 즉시 종료 (30초 카운트다운)

✅ **로그 관리**:
- 중요 이벤트는 `trading_events.log`에 기록
- 10일 이상 된 로그는 자동 삭제 (선택적)

✅ **성과 분석**:
- `generate_trading_report.py` 실행하여 HTML 보고서 생성
- 수익률, 승률, 종목별 분석 등 확인

---

## 📞 추가 자동화

### 로그 자동 정리 (선택)

**작업 이름**: `TradingBot_Cleanup_Logs`
**시간**: 매일 `22:00`
**동작**:
```
프로그램: python.exe
인수: cleanup_old_logs.py
시작 위치: C:\Users\[사용자명]\trading_bot
```

### 일일 보고서 자동 생성 (선택)

**작업 이름**: `TradingBot_Generate_Report`
**시간**: 매일 `21:00`
**동작**:
```
프로그램: python.exe
인수: generate_trading_report.py
시작 위치: C:\Users\[사용자명]\trading_bot
```

보고서는 `reports/trading_report_YYYYMMDD_HHMMSS.html` 형식으로 저장됩니다.

---

## 💡 팁

1. **처음에는 테스트 모드로**:
   - 작업 스케줄러에서 각 작업을 수동 실행하여 정상 작동 확인
   - 실제 자동 실행 전에 최소 1주일간 테스트

2. **알림 설정**:
   - 작업 속성 → 설정 → "작업 실패 시 이벤트 로그에 기록" 체크
   - 이메일 알림은 Windows Server에서만 가능 (Home/Pro 버전 불가)

3. **백업**:
   - 작업 내보내기: 작업 우클릭 → 내보내기 → XML 저장
   - 나중에 다른 컴퓨터에서 가져오기 가능

4. **모니터링**:
   - 매일 `logs/trading_events.log` 확인
   - 주간 성과 보고서 확인

---

이제 완전 자동화된 주식 매매 시스템이 완성되었습니다! 🚀
